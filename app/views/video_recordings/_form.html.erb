<%= form_with(model: video_recording) do |form| %>
  <% if video_recording.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(video_recording.errors.count, "error") %> prohibited this video_recording from being saved:</h2>

      <ul>
        <% video_recording.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
